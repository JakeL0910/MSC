# Copy this file to `.env.local` for local development, or set these in
# Vercel → Project → Settings → Environment Variables for the live site.

# Shared credentials for the private-preview password gate (see src/middleware.ts).
# Change these to whatever you want to give volunteers.
SITE_USER=msc
SITE_PASSWORD=msc2026

# Set to "true" to turn the password gate OFF (make the site fully public).
# GATE_DISABLED=true
